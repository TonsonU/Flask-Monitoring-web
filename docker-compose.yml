version: '3.8'

services:
  web:
    build: .
    container_name: flask_app
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:///instance/database.db
    volumes:
      - .:/app
    restart: always
